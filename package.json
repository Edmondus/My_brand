{
    "name": "my-brand",
    "version": "0.1.0",
    "scripts": {
      "postinstall": "npm install --prefix server && npm run build --prefix server"
    },
    "engines": {
        "node": "14.1.x"
      }
  }